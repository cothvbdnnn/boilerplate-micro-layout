const state = {
  data: []
}

const getters = {
  data: (state) => {
    return state.data
  },
}

const mutations = {
}

const actions = {
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}